# Acme — demo app

Neutral target app (React + Primer) that fidelity-ladder expeditions modify. Built on Primer so
every generated artifact reads GitHub-native. Feature flags live in `flags.json` (stage/prod);
rung 5 ships a change flag-off in prod until human sign-off. Deployed to
`stage.` and `prod.fidelity-ladder-system.n8plusus.com` at P2/P3.
