{
  "name": "acme-demo-app",
  "private": true,
  "version": "0.0.1",
  "description": "Neutral Acme app (Primer) — the target expeditions modify. Feature flags in flags.json.",
  "type": "module",
  "scripts": { "dev": "vite", "build": "vite build", "test": "vitest run" },
  "dependencies": { "@primer/react": "^37", "react": "^18", "react-dom": "^18" },
  "devDependencies": { "vite": "^5", "vitest": "^2" }
}
